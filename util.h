#ifndef UTIL_H
#define UTIL_H
#define max(x, y) (((x) > (y)) ? (x) : (y))
#define min(x, y) (((x) < (y)) ? (x) : (y))
#define distance(a, b) (((a) >= (b)) ? ((a) - (b)) : ((b) - (a)))
#endif // UTIL_H
